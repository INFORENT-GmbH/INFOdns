name: Deploy — hel1 (secondary)

on:
  push:
    branches: [main]
    paths:
      - 'bind/secondary-hel1/**'
      - 'bind/tsig.key'
      - '.github/workflows/deploy-hel1.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up SSH
        run: |
          install -m 600 -D /dev/null ~/.ssh/id_ed25519
          printf '%s' "${{ secrets.SSH_KEY }}" | base64 -d > ~/.ssh/id_ed25519
          ssh-keyscan -H hel1.dns.inforant.de >> ~/.ssh/known_hosts

      - name: Parse host and path from SSH_PATH_HEL1
        id: parse
        run: |
          SSH_PATH="${{ vars.SSH_PATH_HEL1 }}"
          echo "host=${SSH_PATH%%:*}" >> $GITHUB_OUTPUT
          echo "path=${SSH_PATH#*:}" >> $GITHUB_OUTPUT

      - name: Sync BIND config to hel1
        run: |
          rsync -az --delete \
            bind/secondary-hel1/ ${{ vars.SSH_PATH_HEL1 }}/

      - name: Reload BIND on hel1
        run: |
          ssh ${{ steps.parse.outputs.host }} "
            named-checkconf ${{ steps.parse.outputs.path }}/named.conf &&
            rndc reconfig
          "
